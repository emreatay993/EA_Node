# Integrations Requirements

## Built-In Integration Families
- `REQ-INT-001`: Core built-ins shall remain available without optional third-party integration dependencies.
- `REQ-INT-002`: The repo shall ship built-in integration families for spreadsheet, file I/O, email, process execution, and optional domain-specific integrations through the shared registry/bootstrap path.
- `REQ-INT-003`: Spreadsheet integrations shall cover CSV/XLSX read/write workflows with dependency-gated behavior.
- `REQ-INT-004`: File integrations shall cover text/JSON read/write workflows with path-oriented node contracts.
- `REQ-INT-005`: Email integrations shall cover SMTP/TLS/auth send workflows with explicit configuration.
- `REQ-INT-007`: Built-in integration nodes shall stay grouped under the integrations catalog/bootstrap surface rather than ad-hoc registration paths.
- `REQ-INT-008`: The PyDPF viewer integration surface shall treat `ansys-dpf-core` as the analysis backend and `pyvista`, `pyvistaqt`, plus `vtk` as optional viewer extras; shipped built-ins include foundational helper and workflow nodes `dpf.result_file`, `dpf.model`, `dpf.scoping.mesh`, `dpf.scoping.time`, `dpf.mesh_extract`, and `dpf.export`, the foundational operator-backed nodes `dpf.result_field` and `dpf.field_ops`, generated `dpf.op.*` operator wrappers, and `dpf.viewer`; the retained nested-category proof continues to cover the earlier `("Ansys DPF", "Compute")` and `("Ansys DPF", "Viewer")` surface, while the shipped Ansys DPF catalog taxonomy now uses `category_path` with `("Ansys DPF", "Inputs")`, `("Ansys DPF", "Workflow")`, `("Ansys DPF", "Helpers", ...)`, `("Ansys DPF", "Operators", ...)`, and `("Ansys DPF", "Viewer")` as the visible root families and leaves only `("Ansys DPF", "Advanced", "Raw API Mirror")` deferred; `dpf.viewer` is the first concrete `backend_id` implementation of the generic execution-backend and shell-binder framework (`dpf_embedded`), and repo-local `.rst` / `.rth` fixtures under `tests/ansys_dpf_core/example_outputs/` remain the authoritative smoke inputs for packet-owned verification.
- `REQ-INT-009`: The operator-backed Ansys DPF rollout surface shall keep `ansys-dpf-core` optional, lazily register the shipped DPF descriptors only when the backend is available, apply a version-aware plugin lifecycle for the built-in descriptor cache, normalize operator metadata and generated-port semantics into stable registry/runtime descriptors, publish generated operator and helper descriptors with source-family provenance, route shipped operator-backed compute nodes through the generic DPF runtime adapter, materialize helper-provided model, scoping, container, and generic object handles at runtime, preserve reopened `dpf.*` nodes as read-only missing-plugin placeholders when the plugin is unavailable, and defer only the broad non-operator `ansys.dpf.core` reflection surface under `Ansys DPF > Advanced > Raw API Mirror` to a later packet set.
- `REQ-INT-010`: `dpf.viewer` content fullscreen shall retarget the existing native viewer widget and overlay host into the shell `contentFullscreenViewerViewport` when live data is available, restore the same widget to the node viewport on close, workspace switch, or node deletion, and keep blocked or rerun-required viewer states as explicit shell projections without creating a second viewer widget for the same `(workspace_id, node_id)`.
- `REQ-INT-011`: built-in add-on integrations shall flow through the generic add-on catalog and shell-manager contract: each add-on publishes stable id, name, version, category, dependency, and availability facts plus exactly one apply policy (`hot_apply` or `restart_required`), while shell open-manager requests and missing-node ownership resolve against add-on ids instead of ad hoc plugin package names.
- `REQ-INT-012`: `ANSYS DPF` shall ship as the first repo-local `hot_apply` add-on under `ea_node_editor.addons.ansys_dpf`, remain optional at startup, and participate in registry, runtime, and viewer rebuild so disabling it projects live DPF nodes into locked placeholders and re-enabling it restores live DPF availability without a full app restart.

## Acceptance
- `AC-REQ-INT-008-01`: DPF catalog, generated operator and helper, viewer-backend, binder, packaging, taxonomy, and docs/traceability regressions confirm the optional PyDPF viewer stack stays opt-in, the retained `Ansys DPF > Compute` / `Ansys DPF > Viewer` proof remains documented, the path-backed `Ansys DPF > Inputs`, `Ansys DPF > Workflow`, `Ansys DPF > Helpers`, `Ansys DPF > Operators`, and `Ansys DPF > Viewer` taxonomy remains discoverable, its backend-specific binder remains discoverable through the registry wiring, and `docs/specs/perf/CROSS_PROCESS_VIEWER_BACKEND_FRAMEWORK_QA_MATRIX.md`, `docs/specs/perf/NESTED_NODE_CATEGORIES_QA_MATRIX.md`, plus `docs/specs/perf/ANSYS_DPF_FULL_PLUGIN_ROLLOUT_QA_MATRIX.md` name the retained proof.
- `AC-REQ-INT-009-01`: `docs/DPF_OPERATOR_PLUGIN_BACKEND_REVIEW_2026-04-12.md`, `docs/specs/perf/DPF_OPERATOR_PLUGIN_BACKEND_REFACTOR_QA_MATRIX.md`, `docs/specs/perf/ANSYS_DPF_FULL_PLUGIN_ROLLOUT_QA_MATRIX.md`, and the retained plugin-loader, DPF catalog, generated operator/helper, runtime, serializer, and traceability regressions confirm the version-aware optional backend contract, normalized descriptor semantics, generic operator invocation seam, helper object-handle portability, and missing-plugin placeholder portability remain aligned.
- `AC-REQ-INT-010-01`: viewer-host service, embedded-overlay-manager, content-fullscreen bridge, and viewer-surface regressions confirm live DPF viewer fullscreen retarget/restore, blocked-state fallback, workspace switch cleanup, and no orphaned overlay containers, with retained proof summarized in `docs/specs/perf/MEDIA_VIEWER_CONTENT_FULLSCREEN_QA_MATRIX.md`.
- `AC-REQ-INT-011-01`: plugin-loader, main-window shell, presenter/QML, serializer, and closeout-doc regressions confirm the generic add-on catalog, apply-policy metadata, shell focus-target plumbing, and missing-node ownership remain aligned, with retained proof summarized in `docs/specs/perf/ADDON_MANAGER_BACKEND_PREPARATION_QA_MATRIX.md`.
- `AC-REQ-INT-012-01`: DPF catalog, generated operator/helper, runtime, viewer-host, binder, and closeout-doc regressions confirm the repo-local `ANSYS DPF` add-on package, hot-apply lifecycle, and placeholder rebind behavior remain aligned, with retained proof summarized in `docs/specs/perf/ADDON_MANAGER_BACKEND_PREPARATION_QA_MATRIX.md`.
