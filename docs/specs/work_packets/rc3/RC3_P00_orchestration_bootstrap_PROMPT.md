Implement `RC3_P00_orchestration_bootstrap.md` exactly.

Constraints:
- Do not modify runtime/source code outside docs orchestration scope.
- Ensure every RC3 packet has both contract and prompt files.
- Keep packet order strictly sequential and aligned with branch labels in `RC3_STATUS.md`.

Deliverables:
1. RC3 manifest and status ledger.
2. RC3 packet contract/prompt scaffold (`P00` through `P08`).
3. `docs/specs/INDEX.md` entries for RC3 manifest and status.
4. Verification command output summary.
