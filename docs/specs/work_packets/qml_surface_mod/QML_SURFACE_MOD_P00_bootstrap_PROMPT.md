Implement `QML_SURFACE_MOD_P00_bootstrap.md` exactly.

Before editing:
1. Read `docs/specs/work_packets/qml_surface_mod/QML_SURFACE_MOD_MANIFEST.md`.
2. Read `docs/specs/work_packets/qml_surface_mod/QML_SURFACE_MOD_STATUS.md`.
3. Read `docs/specs/work_packets/qml_surface_mod/QML_SURFACE_MOD_P00_bootstrap.md`.

Constraints:
- Implement only `P00` on branch `codex/qml-surface-mod/p00-bootstrap`.
- Change only `docs/specs/work_packets/qml_surface_mod/*` and `docs/specs/INDEX.md`.
- Do not modify `ea_node_editor/**`, `tests/**`, or `docs/specs/requirements/**`.

Deliverables:
1. Full `QML_SURFACE_MOD` packet contract/prompt set for `P00..P09`.
2. Manifest + status ledger with `P00=PASS`, `P01..P09=PENDING`.
3. `QML_SURFACE_MOD_P00_FILE_GATE_PASS` verification output summary.

Stop after `P00`. Do not start `P01`.
