Implement `RC2_P01_shell_theme.md` exactly.

Constraints:
- Preserve object names used by tests (`mainToolbar`, `workspaceTabs`, `consolePanel`, etc.).
- Keep status bar API contract unchanged.
- Move static stylesheet to generated theme tokens (`stitch_dark_v1`).

Deliverables:
1. Code changes in allowed files only.
2. New `tests/test_theme_shell_rc2.py`.
3. Command output summary for verification command.
4. `shell_idle.png` artifact path.

