Implement `RC2_P03_library_inspector.md`.

Constraints:
- Keep text/category/port direction/type filtering semantics unchanged.
- Keep `node_add_requested`, `property_changed`, `port_exposed_changed`, and `collapse_changed` signal contracts.

Deliverables:
1. Categorized library UX with icon-rich rows.
2. Structured inspector sections.
3. Passing tests plus any new tests for category expand/add flow.
4. `library_inspector.png` artifact path.

