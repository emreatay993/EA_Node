Implement `RC2_P06_decorator_sdk.md`.

Constraints:
- Keep `NodeRegistry.register(factory)` behavior unchanged.
- Decorator outputs must still map to strict `NodeTypeSpec`/`PortSpec`/`PropertySpec`.
- Convert at least one built-in node as reference.

Deliverables:
1. `ea_node_editor/nodes/decorators.py`.
2. Reference built-in plugin using decorators.
3. New decorator tests.
4. Spec documentation update.

