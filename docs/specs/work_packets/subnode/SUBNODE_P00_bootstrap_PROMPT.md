Implement `SUBNODE_P00_bootstrap.md` exactly.

Before editing:
1. Read `docs/specs/work_packets/subnode/SUBNODE_MANIFEST.md`.
2. Read `docs/specs/work_packets/subnode/SUBNODE_STATUS.md`.
3. Read `docs/specs/work_packets/subnode/SUBNODE_P00_bootstrap.md`.

Use branch: `codex/subnode/p00-bootstrap`.

Implement only this packet.
Run the packet's listed verification command(s).
Update `docs/specs/work_packets/subnode/SUBNODE_STATUS.md`.
Stop without starting the next packet.
