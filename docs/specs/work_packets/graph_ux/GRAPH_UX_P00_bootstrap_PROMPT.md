Implement `GRAPH_UX_P00_bootstrap.md` exactly.

Before editing:
1. Read `docs/specs/work_packets/graph_ux/GRAPH_UX_MANIFEST.md`.
2. Read `docs/specs/work_packets/graph_ux/GRAPH_UX_STATUS.md`.
3. Read `docs/specs/work_packets/graph_ux/GRAPH_UX_P00_bootstrap.md`.

Constraints:
- Implement only `P00`.
- Keep changes inside `docs/specs/work_packets/graph_ux/*` and `docs/specs/INDEX.md`.
- Do not modify `ea_node_editor/**`, `tests/**`, or `docs/specs/requirements/**`.

Deliverables:
1. Graph UX manifest, status ledger, and packet contract/prompt files for `P00` through `P08`.
2. `docs/specs/INDEX.md` entries for the Graph UX manifest and status ledger.
3. Verification command output summary.
4. `GRAPH_UX_STATUS.md` update marking `P00` as `PASS`.

Stop after `P00`. Do not start `P01`.
