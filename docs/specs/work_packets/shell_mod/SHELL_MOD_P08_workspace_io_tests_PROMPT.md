Implement `SHELL_MOD_P08_workspace_io_tests.md` exactly.

Before editing:
1. Read `docs/specs/work_packets/shell_mod/SHELL_MOD_MANIFEST.md`.
2. Read `docs/specs/work_packets/shell_mod/SHELL_MOD_STATUS.md`.
3. Read `docs/specs/work_packets/shell_mod/SHELL_MOD_P08_workspace_io_tests.md`.

Constraints:
- Implement only `P08` on branch `codex/shell-mod/p08-workspace-io-tests`.
- Extract custom workflow/package import/export methods from controller.
- Split oversized shell/controller tests into focused modules, while preserving module entrypoints used by existing commands.

Deliverables:
1. IO helper module integration and final controller composition.
2. Test modularization with unchanged behavior coverage.
3. Final verification output summary.
4. `SHELL_MOD_STATUS.md` update for `P08`.
