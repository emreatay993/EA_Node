Implement `SHELL_MOD_P00_bootstrap.md` exactly.

Before editing:
1. Read `docs/specs/work_packets/shell_mod/SHELL_MOD_MANIFEST.md`.
2. Read `docs/specs/work_packets/shell_mod/SHELL_MOD_STATUS.md`.
3. Read `docs/specs/work_packets/shell_mod/SHELL_MOD_P00_bootstrap.md`.

Constraints:
- Implement only `P00` on branch `codex/shell-mod/p00-bootstrap`.
- Change only `docs/specs/work_packets/shell_mod/*` and `docs/specs/INDEX.md`.
- Do not modify `ea_node_editor/**`, `tests/**`, or `docs/specs/requirements/**`.

Deliverables:
1. Full `SHELL_MOD` packet contract/prompt set for `P00..P08`.
2. Manifest + status ledger with `P00=PASS`, `P01..P08=PENDING`.
3. `SHELL_MOD_P00_FILE_GATE_PASS` verification output summary.

Stop after `P00`. Do not start `P01`.
